# 詩吟ポータルサイト β公開前チェックリスト

作成日: 2026-07-11
最終更新: 2026-07-13（仕上げ班・polish-aブランチ）
対象ブランチ: polish-a（公開後の磨き込み改修。完了後にユーザー確認→mainへマージ＆push予定）

このファイルは、公開前の最終確認内容と、公開後にheyheyさんご自身で対応いただきたい項目をまとめたものです。

---

## 1. Googleフォーム（掲載情報の修正・お問い合わせ）: 実URL設定済み

以前は仮のURLでしたが、運営者が実際にGoogleフォームを作成し、**実URLに差し替え済み**です。

- 実URL: `https://docs.google.com/forms/d/e/1FAIpQLSfTae1WmuDbTgIqUKCQMHahGo7RJ1gVgEchbMoCmsQEQFbdMA/viewform?usp=dialog`
- 反映済み箇所（すべて同じURLに統一）:
  - `assets/js/links.js`（`requestForm.url` と `contactForms.*.url` 全5種類）
  - `apps/kyoshitsu-map/app.js`（`FORM_URLS.requestForm`）
  - `apps/shigin-jiten/index.html`（`FORM_URLS.requestForm`）
  - `apps/ginshi-archives/index.html`（`requestForm` 定数）
  - `request/index.html`（ボタンのhref直書き）
- 差し替え後、リポジトリ全体を検索し、旧ダミーURLの残存がゼロであることを確認済みです。

このフォーム1つに「教室情報の修正・追加・削除依頼」「講演依頼」「取材依頼」「吟猫道場について」「掲載情報について」「その他のお問い合わせ」をすべて集約する方針です。フォーム側の質問項目で種別を選んでもらう形にすると親切です（未対応の場合はユーザー側でのフォーム調整をおすすめします）。

---

## 2. 書籍情報: 全タイトル確定済み

`assets/js/books.js` で書籍データを一元管理する形に変更しました。

- **メイン3冊**（詩吟の教科書シリーズ）: 入門編・初心者編・中級編とも、タイトル・説明文・AmazonURLすべて確定済みです。
  - 入門編・初心者編には表紙画像も設定済み（`assets/img/legacy/kindle-nyumon.jpg` / `kindle-shoshinsya.jpg`）。中級編は画像なし（テキストカードのまま。表示崩れなし）。
- **小カード4冊**（詩吟で深める漢詩の世界シリーズ・吟猫書房）: タイトルもAmazon商品ページから取得済みで、「タイトル要確認」のプレースホルダーは残っていません。画像は未設定（テキストカードのまま）。
- 書籍を表示している全ページ（トップ・`beginner/`・`newsletter/`）に「このページにはアフィリエイトリンクが含まれます。」の注記があることを確認済みです。

---

## 3. 「準備中」表示のまま未設定になっている項目（ユーザー作業待ち）

- **メルマガ登録の入口別URL（3種類）**: `newsletter.beginner` / `newsletter.learner` / `newsletter.teacher`（`assets/js/links.js` 74〜87行目付近）が空欄のままです。
  - ※「準備中」表示にはならず、現状は自動的に既存メルマガURL（`https://shigin.net/p/r/cbWMf1ve`）にフォールバックしてボタンとして機能しています。読者層別に別々の登録ページを用意したい場合は、それぞれの `url` に登録フォームのURLを設定してください。

---

## 4. 既知の残課題（公開後に順次対応いただく項目）

- **吟猫ピッチマップの使い方ガイド（`apps/pitchmap/guide.html`）**: スクリーンショット画像が未挿入です。各ステップの「ここに画面写真が入ります」という枠に、実際の操作画面のスクリーンショットを差し込む必要があります。HTML内にコメントで差し替え例（`<img src="./guide-images/step2-recorder.png" ...>` など）を残してあるので、`guide-images/` フォルダを作って画像を置き、コメントを外すだけで反映できます。
- **漢詩辞典（`apps/kanshi-jiten/`）にポータルへ戻る導線がない**: 他の4ツール（教室マップ・吟士アーカイブス・詩吟大辞典・ピッチマップ）には「← heyhey 詩吟ポータル」という戻りリンクがありますが、漢詩辞典にはこのリンクが未実装です。ブラウザの「戻る」で対応はできますが、他ツールとの一貫性のため、後日追加をおすすめします。
- **旧ページの和紙テクスチャ画像が欠損**: `about.html` / `blog.html` / `contact.html` / `recommended-videos.html` / `how-to-record.html` の背景CSSが `必要画像/washi-texture.png` を参照していますが、このファイルは存在しません。背景装飾のみのため表示は崩れません（画像なしでも背景色で表示）。正式な画像を用意すると見た目が改善します。

---

## 5. 今回（メルマガ導線強化）の主な変更内容

- 全ページに統一文言・統一ボタン「無料メルマガで7日間学ぶ」でのメルマガ導線を追加（トップ最上部ブロック・ヒーローCTA・各ページ下部の帯など）
- 「オープンチャット」の表記を「吟ねこコミュニティ（LINEのオープンチャット）」に統一
- 書籍カードのデータ駆動化（`assets/js/books.js` 新設）＋メイン2冊に表紙画像を追加
- 教室マップ: 不要なUI案内の削除、修正依頼リンク文言の改善
- 詩吟大辞典: 掲載情報の依頼リンクを追加
- 吟猫ピッチマップ: グラフの凡例を追加（赤線・青線の意味を明記）
- 吟題アーカイブス: リスト表示化、関西吟詩文化協会の音源リンクを12吟題分追加（音源そのものは再配布せず、公式ページへのリンクのみ）

---

## 6. 公開手順について

現在の公開フローは **mainブランチへのpush → GitHub Actions → GitHub Pages自動デプロイ**です（`.github/workflows/deploy.yml`）。公開URL: `https://heyhey3011.github.io/homepage/`
mailmagブランチの内容をユーザー確認後にmainへマージ＆pushすると、自動的に反映されます（pushは必ずユーザーの明示的なOKを得てから行ってください）。

---

## 7. `必要画像/` フォルダについて

`必要画像/`（日本語名）フォルダは、リニューアル後のポータル側では使用していませんが、**削除しないでください**。
`about.html` / `blog.html` / `contact.html` / `recommended-videos.html` / `dojo-details.html` / `how-to-record.html` といった旧ページ側がまだこのフォルダの画像を参照しているため、残置が必要です。

---

## 8. 実施した機械チェックの結果（仕上げ班・2026-07-12）

- 旧ダミーURL（`docs.google.com/forms/.../1FAIpQLSeV5rh...`）の残存: リポジトリ全体を検索し**ゼロを確認**。
- ボタン文言「無料メルマガで7日間学ぶ」: 旧文言「詩吟の始め方を7日間で学ぶ」「7日間講座を無料で受け取る」の残存は**なし**。
- 「オープンチャット」単独表記: `contact/index.html`（2箇所）・`profile/index.html`（1箇所）で「吟ねこコミュニティ」と併記されていない箇所を発見し、「吟ねこコミュニティ（LINEのオープンチャット）」に修正済み。
- アフィリエイト表記: トップ・`beginner/`・`newsletter/` の3ページすべてに存在することを確認。
- 全HTMLのローカルリンク・画像参照: 自動チェックで検出した「broken」候補はすべて誤検知（JS内の文字列連結、テンプレート用コメント、任意ファイルの `config.local.js`）と確認済み。実際に壊れているリンク・画像はなし。
- `node --check` によるJS構文チェック: `assets/js/`・`apps/` 配下すべて**エラーなし**。
- `sitemap.xml`: 今回の作業で新規ページは追加していないため、更新不要と判断。
- `git status`: 意図しないファイル・APIキー等の混入なし（差分内を`api_key`/`secret`/`password`/`token`で検索し該当なし）。

---

## 9. 公開後の磨き込み改修（仕上げ班・2026-07-13、`polish-a`ブランチ）

### 今回（A班・B班・仕上げ班）の主な変更内容

- ヒーローH1「詩吟を、もっと探しやすく、学びやすく、続けやすく。」を「、」区切りで`span`分割し、全画面幅で単語途中改行しないように調整。
- PWA最低限セットを追加: `manifest.json`＋アイコン一式（icon-192/512・apple-touch-icon・favicon-32/16）を全ポータルページの`<head>`に設置。
- 学習ツール（`tools.*`）を開くリンクを別タブ化（`target="_blank" rel="noopener noreferrer"`、`links.js`のJS注入＋一部静的記載）。
- トップ「あなたに合う入口を選んでください」6カードのリンク切れを解消。
- 書籍カード: 画像クリックでもAmazonリンクへ遷移するように修正。
- `tools/`ページを比較表→各ツール詳細の構成に再構成し、アンカーリンクで連携。
- 「吟猫道場を見る」の表記を全ページ「吟猫道場ってなに？」に統一。
- `dojo-details.html`をサイト共通デザイン（`site.css`・共通ヘッダー/フッター）に刷新（月額990円・7つの特長・入会手順・参加者の声など既存の事実情報は維持）。
- YouTube登録者数・動画本数はAPI取得失敗時に`CONFIG.SECURITY.FALLBACK_*`の数値を「約◯◯人」形式で表示するように変更。
- 「漢詩辞典」の名称を「漢詩の図書館」に変更（ポータル側・kanshi-jiten側の両方）。
- apps/配下5ツール共通: 画面右上に「ホームへ戻る」導線を統一設置（kanshi-jitenはDOM注入のfixedオーバーレイボタン、React本体は無改変）。
- kyoshitsu-map: メルマガ帯を下部固定からflow内配置＋閉じるボタン（localStorage記憶）に変更し、地図操作の邪魔にならないよう調整。
- kanshi-jiten: 文字サイズを一段階UPし、小/中/大トグル（localStorage保存）を追加。各漢詩の詳細末尾に「関連音源・動画」枠を追加（データ未登録時は「準備中です」と表示、`window.KANSHI_MEDIA_LINKS`に将来のデータ紐付け用の空マッピングを用意）。
- pitchmap `guide.html`: 画像挿入エリアの表記を「画像準備中」に統一。
- ginshi-archives: タブ順を「動画一覧」「吟士名鑑」「吟題一覧」に変更。

### 仕上げ班の機械チェック・実機確認結果（2026-07-13）

- 全ポータルページ＋apps5ツールのリンク・画像参照を自動チェックし、A班・B班の変更箇所（manifest.json・アイコン各種・kanshi-jitenの新規挿入部分）を含めて実際に壊れている参照はなし。
- 旧ページ（`about.html`・`blog.html`・`contact.html`・`how-to-record.html`・`recommended-videos.html`）にPWA用の`manifest`/`favicon`/`apple-touch-icon`タグが未設置だったため、仕上げ班にて追加し、全ページで200応答することを確認済み。
- 主要CTA（教室マップ・7日間講座・コミュニティ・問い合わせ・修正削除依頼、入口カード6枚、書籍カードのAmazonリンク、tools比較表アンカー、apps5ツールの戻るボタン・機能）を実機（ローカルサーバー）で動作確認済み。詳細はコミットメッセージ・報告参照。
- スマホ幅375pxで、ポータル12ページ（`index.html`＋11サブページ）＋`dojo-details.html`＋apps5ツール、合計18ページの横はみ出し（`scrollWidth > innerWidth`）なしを確認済み。

---

## 10. 今後のTODO（優先度C・未実装）

今回の磨き込み改修では対応していない、公開後に順次対応いただきたい項目です。

- **YouTube統計の自動取得の安定化**: 現在はAPIキー未設定時に固定のフォールバック値（約◯◯人・約◯◯本）を表示する仕組みのみ。APIキーを設定した場合の自動取得の安定動作（レート制限対策・エラーハンドリング強化）は未着手。
- **漢詩ごとの音源・動画リンクの大量登録**: kanshi-jitenに「関連音源・動画」枠を追加したが、現状は`window.KANSHI_MEDIA_LINKS`が空のマッピングのままで、各漢詩に対応する音源・動画は未登録（表示は「準備中です」）。
- **吟士名鑑の本格整備**: ginshi-archivesのタブ「吟士名鑑」は存在するが、内容の本格的な整備（吟士情報の充実）は今後の課題。
- **吟題一覧の本格整備**: 同じくginshi-archivesの「吟題一覧」タブも、内容の充実は今後の課題。
- **各ツールの管理者編集機能**: kyoshitsu-map・pitchmap・kanshi-jiten・ginshi-archives・shigin-jitenのいずれも、データの追加・修正は現状ファイル直接編集のみ。管理画面からの編集機能は未実装。
- **各ツールのデザイン全面統一**: apps/配下5ツールはそれぞれ異なる時期・方針で作られており、デザインのトンマナが統一されていない。ポータル本体の`site.css`デザインシステムへの統一は今後の課題。
